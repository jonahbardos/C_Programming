#include <stdio.h> // Tells the compiler to include information about the standing input and output library.

// This function must be in every C Program because it is a COMPILER!
// the "main" function is where all the codes gets executed.
int main(){ 
    printf("Hello, World!\n");
    return 0;
}